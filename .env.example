# コピーして .env.local を作成し、値を埋める（.env.local は Git に含めない）
# Vercel 本番: 同じ変数名を Project → Settings → Environment Variables に登録

# DB（Vercel / Prisma Postgres）
POSTGRES_URL=
PRISMA_DATABASE_URL=

# NextAuth
GITHUB_ID=
GITHUB_SECRET=
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
